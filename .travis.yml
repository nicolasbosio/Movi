
language: php
php:
  - '5.6'

before_script:
  - wget http://getcomposer.org/composer.phar
  - php composer.phar install

script:
  - mkdir -p build/logs
  - phpunit test/TestTarjeta.php

after_success:
  - travis_retry php coveralls
